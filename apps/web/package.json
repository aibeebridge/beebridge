{
  "name": "@beebridge/web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf .next",
    "clean:cache": "rm -rf ../../node_modules/.cache",
    "dev": "next dev",
    "dev:fresh": "npm run clean && next dev",
    "build": "next build",
    "build:clean": "npm run clean && next build",
    "build:fresh": "npm run clean && npm run clean:cache && next build"
  },
  "dependencies": {
    "@beebridge/shared": "0.1.0",
    "@xyflow/react": "^12.10.2",
    "next": "^15.3.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@types/react": "^19.0.10",
    "typescript": "^5.8.2"
  }
}
